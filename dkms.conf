PACKAGE_NAME="alg-rgb"
PACKAGE_VERSION="0.1.3"

BUILT_MODULE_NAME[0]="alg_rgb"
BUILT_MODULE_LOCATION[0]="kernel"
DEST_MODULE_LOCATION[0]="/updates/dkms"

MAKE[0]="make -C /usr/lib/modules/${kernelver}/build M=${dkms_tree}/${PACKAGE_NAME}/${PACKAGE_VERSION}/build/kernel modules"

AUTOINSTALL="yes"
